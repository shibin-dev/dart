void main() {
  List numbers = [3, 6, 7, 8, 12, 34];
  //first for loop
  // for (var i = 0; i < numbers.length; i++) {
  //   print(numbers[i]);
  // }

  //second for loop
  // for (var number in numbers) {
  //   print(number);
  // }

  //third for loop
  // numbers.forEach((element) {
  //   print(element);
  // });

  //reduce
  // final val = numbers.reduce((value, element) => value + element);
  // print(val);

  //fold
  // final foldValue = numbers.fold<double>(
  //     10, (previousValue, element) => previousValue + element);
  // print(foldValue);
}
